name: Validate CFG Files

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
    paths:
      - '**.cfg'
  push:
    branches: [ master ]
    paths:
      - '**.cfg'

jobs:
  validate:
    uses: KSPModdingLibs/KSPBuildTools/.github/workflows/validate.yml@1.1.1
